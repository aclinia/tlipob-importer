[project]
name = "tlipob-importer"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "easyocr>=1.7.2",
    "pyside6-essentials>=6.10.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.1",
]
